# Local environment overrides
# This file is automatically loaded by Symfony and overrides .env values

# Disable Symfony Runtime (we use traditional bootstrap)
SYMFONY_RUNTIME_DISABLE=1

# Additional local development settings
APP_ENV=dev
APP_DEBUG=1
